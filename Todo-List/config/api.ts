// Cambia esto por tu IP local
// Para encontrarla, abre una terminal y ejecuta: ipconfig
// Busca "Dirección IPv4" en la sección de tu conexión de red

const API_IP = '10.10.151.195'; // ✅ TU IP
const API_PORT = '3000';

export const API_URL = `http://${API_IP}:${API_PORT}`;

// Ejemplo:
// Si necesitas cambiarla, reemplaza el valor de API_IP
